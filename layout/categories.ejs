<%- partial('_partial/header.ejs') %> 
<% if (site.categories.length){ %>
    <div class="container post-card p-5">
      <h3 class="title-divider"><%= __('categories') %></h3>
        <div class="card-narrow-content">
          <%- list_categories(site.categories, {
            show_count: true,
            class: 'timeline',
          }) %>
        </div>
    </div>
<% } %>
    