<div class="tag-bar primary-color">
    <% if (post.tags && post.tags.length){ %>
        <%- list_tags(post.tags, {
            show_count: false,
            class: 'tag'
          }) %>
    <% } %>
</div>